[package]
name = "kash"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["lib", "cdylib"]

[dependencies]
serde = { version = "1.0.139", features = ["derive"] }

[features]
default = [
    "fixed",
    "income"
]
fixed = []
income = []
